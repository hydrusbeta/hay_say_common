# This requirements file is not part of the package. It's just here to make it easier to install dependencies for local
# development and testing.

flask==2.0.3
librosa==0.8.1
soundfile==0.10.3.post1
